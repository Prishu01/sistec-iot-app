services:
  - type: web
    name: sistec-iot-app
    env: node
    buildCommand: npm ci && npm rebuild sqlite3 --build-from-source && npm run build
    startCommand: npm start
    envVars:
      - key: NODE_VERSION
        value: 22
